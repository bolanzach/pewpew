#include "line.h"

// void draw_line_dda(int x0, int x1, int y0, int y1, u_int32_t color) {
//     // int delta_x = x1 - x0;
//     // int delta_y = y1 - y0;

//     // // which side (x, y) is longer
//     // int side_length = abs(delta_x) >= abs(delta_y) ? abs(delta_x) : abs(delta_y);

//     // // How much we should increment x and y
//     // float x_increment = delta_x / (float) side_length;
//     // float y_increment = delta_y / (float) side_length;

//     // float current_x = x0;
//     // float current_y = y0;

//     // for (int i = 0; i <= side_length; i++) {
//     //     //
//     // }
// }