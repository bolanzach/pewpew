// #ifndef LINE_H
// #define LINE_H

// #include <stdint.h>


// // y = mx + c
// //
// // m: adjusts the y slope. The larger m, the steeper the slope
// // y: where the line _intercepts_ the y-axis
// //
// // m is a ratio of DetlaY / DeltaX. m > 1 means the y
// // grows faster than x. m = 1 = 45 degrees.

// void draw_line_dda(int x0, int y0, int x1, int y1, uint32_t color);

// #endif